version: "3"

services:
  waggle-service:
    platform: linux/amd64
    restart: always
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3001:80"
    stdin_open: true

  # nginx:
  #   image: nginx:latest
  #   build:
  #     context: .
  #     dockerfile: Dockerfile
  #   ports:
  #     - "80:80"
  #   volumes:
  #     - ./nginx/nginx.conf:/etc/nginx/nginx.conf
