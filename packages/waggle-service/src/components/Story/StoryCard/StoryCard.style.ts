import { css } from "@emotion/react";

export const imgStyle = css({
  width: "100%",
  height: "100%",
  objectFit: "cover",
  borderRadius: "4px",
});

export const iconStyle = css({
  position: "absolute",
  top: "12px",
  right: "12px",
});
